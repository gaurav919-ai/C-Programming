// Write a program to show the difference between: pre-increment (++i), post-increment (i++)
#include <stdio.h>

int main() {
    int i = 5;

    printf("Pre increment: %d\n", ++i);
    printf("Post increment: %d\n", i++);

    return 0;
}
