// Write a program that uses scanf() to take input and correctly stores the value in a variable.
#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);
    printf("You entered: %d\n", x);
    return 0;
}
